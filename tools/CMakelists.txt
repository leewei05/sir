get_property(dialect_libs GLOBAL PROPERTY MLIR_DIALECT_LIBS)
get_property(conversion_libs GLOBAL PROPERTY MLIR_CONVERSION_LIBS)
set(LIBS
        ${dialect_libs}
        ${conversion_libs}
        MLIRRegisterAllPasses
        MLIRRegisterAllDialects
        MLIRArithDialect
        MLIROptLib
        MLIRSIR
        )
add_llvm_executable(sir-opt sir-opt.cpp PARTIAL_SOURCES_INTENDED)

llvm_update_compile_flags(sir-opt)
target_link_libraries(sir-opt PRIVATE ${LIBS})

mlir_check_all_link_libraries(sir-opt)
