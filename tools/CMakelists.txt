get_property(dialect_libs GLOBAL PROPERTY MLIR_DIALECT_LIBS)
get_property(conversion_libs GLOBAL PROPERTY MLIR_CONVERSION_LIBS)
set(LIBS
        ${dialect_libs}
        ${conversion_libs}
        MLIRArithDialect
        MLIROptLib
        MLIRSparkIR
        )
add_llvm_executable(spark-ir-opt spark-ir-opt.cpp PARTIAL_SOURCES_INTENDED)

llvm_update_compile_flags(spark-ir-opt)
target_link_libraries(spark-ir-opt PRIVATE ${LIBS})

mlir_check_all_link_libraries(spark-ir-opt)
