set(SIR_TOOLS_DIR "${CMAKE_BINARY_DIR}/tools")

configure_lit_site_cfg(
        ${CMAKE_CURRENT_SOURCE_DIR}/lit.site.cfg.py.in
        ${CMAKE_CURRENT_BINARY_DIR}/lit.site.cfg.py
        MAIN_CONFIG
        ${CMAKE_CURRENT_SOURCE_DIR}/lit.cfg.py
)

set(SIR_TEST_DEPENDS
        sir-opt
        )

add_lit_testsuite(check-sir "Running the SIR regression tests"
        ${CMAKE_CURRENT_BINARY_DIR}
        DEPENDS ${SIR_TEST_DEPENDS}
        )

set_target_properties(check-sir PROPERTIES FOLDER "Tests")

add_lit_testsuites(SIR ${CMAKE_CURRENT_SOURCE_DIR} DEPENDS ${SIR_TEST_DEPENDS})
